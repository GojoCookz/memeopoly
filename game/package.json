{
  "name": "memeopoly",
  "version": "1.0.0",
  "description": "Memeopoly - A meme-powered multiplayer board game with $MEMO tokens",
  "main": "server/server.js",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "HTTP=true node server/server.js",
    "dev": "HTTP=true node server/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "memeopoly",
    "multiplayer",
    "board-game",
    "solana"
  ],
  "license": "MIT",
  "dependencies": {
    "node-static": "^0.7.11",
    "ws": "^3.3.2"
  }
}
